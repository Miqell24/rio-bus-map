{
  "name": "rio-bus-map",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Interactive map of Rio de Janeiro municipal buses and the BRT (SMTR) — GTFS + OSM + HMM map matching",
  "scripts": {
    "download": "bash pipeline/download.sh",
    "build": "node --max-old-space-size=8192 pipeline/build.mjs --all",
    "report": "node pipeline/report-gaps.mjs",
    "serve": "node pipeline/serve.mjs 8148"
  }
}
